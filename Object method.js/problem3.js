//create a object and inside write a function which should add two no. and return them.

let obj={
  add:function (a,b){
    sum=a+b;
      return sum;
  }
}

let output=obj.add(2,3);
console.log(output);