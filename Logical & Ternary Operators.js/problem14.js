// vowel=a,e,i,o,u

let character="j";

if((character=="a") || (character=="e") || (character=="i") || (character=="o") || (character=="u")){
  console.log("Vowel");
}
else {
  console.log("Not a Vowel")
}